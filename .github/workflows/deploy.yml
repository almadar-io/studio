name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # Cache Docusaurus build artifacts: webpack5's filesystem cache
      # (node_modules/.cache/webpack) and Docusaurus's plugin output
      # (.docusaurus). On a warm hit webpack reuses ~95% of compiled
      # modules, dropping a long build down to a few minutes.
      - name: Cache Docusaurus build artifacts
        uses: actions/cache@v4
        with:
          path: |
            .docusaurus
            node_modules/.cache
          key: docusaurus-${{ runner.os }}-${{ hashFiles('package-lock.json', 'docusaurus.config.ts', 'shared/**', 'docs/**', 'blog/**', 'src/**', 'static/**') }}
          restore-keys: |
            docusaurus-${{ runner.os }}-

      - run: npm ci
      - run: npx docusaurus build

      - name: Authenticate to Firebase
        run: echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > /tmp/firebase-sa.json

      - name: Deploy to Firebase
        run: npx firebase-tools deploy --only hosting --project kflow-b3a39
        env:
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/firebase-sa.json

      - name: Cleanup
        if: always()
        run: rm -f /tmp/firebase-sa.json
